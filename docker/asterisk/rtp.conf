; RTP media port range. Kept small so it can be published in docker-compose;
; must match the compose port mapping (10000-10100/udp). Widen both together
; if you run many simultaneous test calls.
[general]
rtpstart=10000
rtpend=10100
